export const DuelFinished = ({ duel }) => {
  console.log(duel);
  return (
    <div className="flex flex-col gap-3 md:justify-between w-full">
      this duel has is finished. show results.
    </div>
  );
};
